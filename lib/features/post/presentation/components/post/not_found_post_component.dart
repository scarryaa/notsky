import 'package:flutter/widgets.dart';

class NotFoundPostComponent extends StatelessWidget {
  const NotFoundPostComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox.shrink();
  }
}
