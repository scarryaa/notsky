import 'package:flutter/widgets.dart';

class BlockedPostComponent extends StatelessWidget {
  const BlockedPostComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Blocked');
  }
}
